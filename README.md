Пароль от БД лежит в файле vault.yml (пароль от него `-=-=-=`).

Плейбук запускается при помощи команды
`ansible-playbook playbook.yml -i hosts --ask-vault-pass` и вводится пароль для расшифровки файла